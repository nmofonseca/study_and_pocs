---
apiVersion: v1
kind: Pod
metadata:
  name: boto3
  namespace: default
spec:
  containers:
  - name: boto3
    imagePullPolicy: Always
    image: nmofonseca/boto3test:v6
    command: [ "/bin/bash", "-c", "--" ]
    args: [ "while true; do sleep 360; done;" ]
    resources:
      requests:
        cpu: "0.05"
        memory: 100Mi
      limits:
        cpu: "0.1"
        memory: 200Mi